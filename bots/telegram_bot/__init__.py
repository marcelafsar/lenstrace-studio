"""Telegram bot interface. Run via ``python -m bots.telegram_bot.main``.

Requires ``python-telegram-bot>=21`` and a ``TELEGRAM_BOT_TOKEN`` environment
variable. Verify the current library docs before extending the handlers.
"""
