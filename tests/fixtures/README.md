# Test fixtures

Test images are **generated at runtime** by `tests/conftest.py` (small solid
colour JPEGs/PNGs), so this project never commits real photos or personal
metadata.

If you add a static fixture, ensure it is a synthetic image with no personal
GPS/EXIF data.
